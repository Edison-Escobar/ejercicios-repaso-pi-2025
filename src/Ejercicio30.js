// 30. Sumar números del arreglo

//Tema: acumulador

const numeros = [5, 10, 15, 20]
let suma = 0

for (i = 0; i < numeros.length ; i++) {
    suma += numeros[i]
    
}
console.log(`La sumer es : ${suma}`)